# 登録カレンダー（AU NEW CAR 登録カレンダー）

車両登録スケジュールを管理するGoogle Apps Script製Webアプリです。GoogleスプレッドシートをDBとし、月ごとにシートを分けて保存します。

## 今回のアップデート内容

### 1. 編集権限をGoogleアカウントで判別
これまでは `k-toda@gifuyanase.co.jp` 一人だけが編集可能で、しかも共有パスワード（平文でコード内にハードコード）が実質使われていない状態でした。

- `Code.gs` の `EDITOR_EMAILS` 配列に登録したGoogleアカウントだけが登録・編集できるようにしました。
- 編集権限の判定は必ずサーバー側（`saveRecord`）で `Session.getActiveUser().getEmail()` を見て行います。画面側のボタン非表示などはあくまで見た目の制御で、実際の権限チェックはサーバー側が最終防衛線です。
- 画面右上（スマホはヘッダー下）に、現在アクセスしているGoogleアカウントと「編集可 / 閲覧のみ」が常に表示されます。
- 閲覧のみのアカウントでは、新規登録の入口（＋新規登録、日付クリック、ドラッグ&ドロップ）が出ず、既存データはクリックすると「閲覧のみ」の詳細モーダル（全項目readonly・保存ボタン非表示）が開きます。

**編集者の追加/削除方法**: `Code.gs` の `EDITOR_EMAILS` にメールアドレスを追記・削除して `clasp push` / デプロイし直してください。パスワードは廃止したので、以前の `ADMIN_PASSWORD` は無効です（このコードがリポジトリに入る前の運用で使われていたパスワードは漏洩済み扱いとして、今後使用しないでください）。

**デプロイ時の注意**: `appsscript.json` は `executeAs: USER_ACCESSING` にしています。これは各ユーザーのGoogleアカウントを正しく判定するために必須の設定です（`USER_DEPLOYING` のままだと全員が同じ権限で動いてしまいます）。Webアプリのデプロイ設定でも「アクセスできるユーザー」を組織内（ドメイン）に限定してください。匿名アクセスを許可すると `Session.getActiveUser().getEmail()` が取得できず、誰も編集できなくなります。

### 2. 「見づらい」の原因調査と改善
複数の方から「見づらい」という声があったため、UIを見直しました。主な原因と対処は以下の通りです。

| 原因 | 内容 | 対処 |
|---|---|---|
| 文字が小さすぎる | 顧客名・拠点・KPIラベルなどが `7px〜9px` で組まれており、老眼や離れた画面では判読が困難 | 最小サイズを `10px` 前後まで底上げし、顧客名やKPIの数値などの主要情報は `text-sm`（14px）〜 `text-2xl/3xl` に拡大 |
| 文字間が広すぎる | 小さい英字ラベルに `tracking-[0.2em]〜[0.25em]` を使っており、余計に間延びして見えた | `tracking-wide` 程度まで縮小 |
| コントラスト不足 | ラベルやカードの補助情報が薄いグレー（`#80868B`/`#70757A`）で、視認性が低かった | `#3C4043` など濃いグレーに変更し、太字も併用 |
| 情報密度が高すぎる | カレンダーの登録カードが高さ74px前後に4行分の情報を詰め込み、拡大すると窮屈で崩れる | カード高さ・カレンダーセルの最小高さを拡張し、拡大後のテキストが収まるように調整 |
| OSS/紙登録が色だけで区別 | 色弱の方や離れた画面では判別しづらい | バッジに `⚡OSS` / `📄PAPER` のようにアイコン＋文言を併記し、色以外でも区別できるように |
| 通信失敗が無言 | データ取得（`fetchData`）が失敗しても画面が空のままで気づけなかった | 取得失敗時にエラーメッセージを表示するようにした |

見た目のブランドロゴ表示も、これまでJavaScriptでヘッダーの文字をDOM操作で書き換える作りだったため、HTML側に直接「AU NEW CAR」を記載する形に整理しました。

### 3. カレンダーのマスが登録件数で詰まる問題
1マスに2件以上入るとカード同士が窮屈になり、内側スクロールで見るしかなかった点を改善しました。

- カレンダーの1マスの高さは登録件数によらず常に固定です。
- 1件だけの日はこれまで通りの詳細カードをそのまま表示します。
- 2件以上ある日は、1件目をコンパクトなカードで表示し、残りは「+N件 すべて見る」ボタンにまとめました。ボタンを押すとその日の全件を一覧できるモーダルが開き（`openDayListModal`）、各カードから通常の編集モーダルにもそのまま遷移できます。

### 4. 未定リストのカードの余白を詰めた
右サイドバー（未定リスト）のカードは高さ固定＋余白多めの作りだったため、固定高さをやめて内容に合わせた余白（`px-3 py-2` ＋ 行間 `gap-1`）に詰め、カード間の間隔も `gap-2` → `gap-1.5` にしました。

### 5. カレンダー／リスト／未定リストの高さを揃え、内部スクロールに統一
カレンダー・リスト表示・未定リストがそれぞれ別の高さで伸び縮みしており、ページ全体がスクロールしてしまう場合がありました。

- カレンダー表示の外枠（`calendar-wrapper`）を `lg:h-[820px]` の固定高さにしました。リスト表示（`view-list-container`）・未定リスト（`sidebar-wrapper`）も同じ高さで揃います。
- それぞれの中身（カレンダーのマス目、リストのテーブル、未定リストのカード）は、この固定高さを超えた分だけ内部スクロールします。ページ自体がその分で伸びることはありません（ヘッダーやダッシュボード部分を含めたページ全体のスクロールは従来どおりです）。
- 対象はPC相当の画面幅（`lg:` = 1024px以上、未定リストが横に並ぶ幅）です。それより狭い画面は今までどおり縦積みの自然な高さのままです。

### 6. 「登録台数」に当月の未定リスト分も含めるように
これまでは登録日が確定した分のみを台数としてカウントしていましたが、今月分の確定台数に加えて、登録日未定（まだ日付が決まっていない）の全件も合算して「登録台数」に表示するようにしました。日付がないため月では絞り込めず、未定分は常に全件を加算します。OSS登録率・OSS登録件数・紙登録件数・拠点別の件数も同じ集計範囲（今月確定分＋未定分）に揃えています。

### 7. PDFリンクを日付に関わらず即反映、月フォルダで誤マッチを防止
- これまでフロント側で「登録日が過去の分だけ」PDFアイコン・PDFへのリンクを表示する作りだったため、実際にはリンクが貼られていても今日以降の日付では見えませんでした。日付に関わらず、リンクが貼られていればカレンダー・リストどちらでもPDFアイコンが表示され、クリックでPDFを開けるようにしました（アイコン以外の部分をクリックすると、これまでどおり編集/詳細モーダルが開きます）。
- `autoLinkVehicleInspectionPDF`（Code.gs）の突き合わせロジックを、部分一致から正規化した氏名の完全一致に変更しました。「山田高」と「山田高市」のような別人を誤って同一視しないようにするためです。
- 車検証PDFの保管フォルダが `2026.08` のような年.月フォルダで分かれている前提で、同姓同名の別人がいる場合は登録予定日が属する月フォルダのPDFを優先して採用します。月フォルダで絞り込めず候補が複数残る場合は、誤リンクを避けるためリンクを空のままにします（すでにリンク済みの行は上書きしません）。
- `autoLinkVehicleInspectionPDF` を15分おきに自動実行するトリガーを追加しました（`setupPdfLinkTrigger`）。GASエディタからこの関数を一度だけ手動実行してトリガーを設置してください。これにより、ドライブにPDFが追加されてから最大15分程度でアプリ側に反映されます。

### 8. ダッシュボードのラベルを日本語表記に変更
「TOTAL REGISTRATIONS」「OSS RATE」「OSS COMPLETED」「PAPER PROCESS」を、それぞれ「登録台数」「OSS登録率」「OSS登録件数」「紙登録件数」に変更しました。単位表記も「UNITS」「CASES」→「台」「件」に揃えています。

### 9. 未定リストからの登録日反映で重複が起きないように
未定リストのカードをカレンダーへドラッグ&ドロップして登録日を反映する処理（`handleDrop`）が、顧客名の文字列一致で対象データを探していたのが原因でした。同姓同名の別レコード（別人の場合はもちろん、リピート客の別案件なども含む）が既に存在すると、ドラッグした本人ではなく別のレコードを誤って更新してしまうことがありました。

- ドラッグ&ドロップは顧客名ではなくレコードのID（ドラッグ開始時に `dataTransfer` へ載せる値をIDに変更）で対象を特定するようにしました。未定リストのモーダルから日付を入力して保存する経路は元々ID指定だったため、これで両方の経路が同じ確実な照合方法に統一されます。
- あわせて、正規化した顧客名が2件以上のレコードで一致する（＝重複の可能性がある）場合に、未定リスト・リスト表示の該当カードに注意アイコン（⚠）を表示するようにしました。ドラッグ&ドロップや未定リストからの入力はもちろん、スプレッドシートを直接編集して発生した重複も含めて検出できます。スプレッドシートへの直接編集そのものはアプリの制御が及ばないため、事前に防ぐことはできませんが、発生に気づけるようにしています。

**スプレッドシートに直接新規行を入力する際のID列について**: **空欄のままで構いません。** データ入力の基本フローが「まずスプレッドシートに直接入力 → それを未定リストから確定させていく」である前提に合わせて、`getRegistrationData`（画面がデータを読み込むたび）が実行するタイミングで、ID列が空欄の行・他の行とIDが重複している行（前の行をコピーして新しい行を作った場合など）を自動検知し、新しいユニークなIDを振ってシートへ書き戻す処理（`ensureUniqueIds_`）を追加しました。ドラッグ&ドロップ等はこのIDで対象データを特定しているため、これによりスプレッドシート直接入力が起点でも常に正しい行だけが更新されます。

### 10. 第三者レビューで見つかった不具合の修正
一通り実装した後、独立した観点でのコードレビューを行い、以下の不具合を修正しました。

- **車検証リンクが消えるおそれ（重大）**: `ensureUniqueIds_`・`autoLinkVehicleInspectionPDF`・`saveRecord` はいずれも、シートの一部のセルだけを書き換えてから行全体を `setValues()` で書き戻す作りでした。読み込みに `getValues()`（計算結果の文字列）だけを使っていたため、書き戻しの対象になった行に含まれる**他のセルの `=HYPERLINK(...)` 数式が、計算結果の文字列に置き換わって壊れてしまう**不具合がありました。例えば、ID列が空欄の行を1件直しただけで、同じシート内の別の行にある正常な車検証リンクが巻き添えで消える、といったことが起こり得ました。数式を保持したまま読み込む共通関数（`getSheetDataPreservingFormulas_`）を用意し、書き戻しを行う3箇所すべてをこれに差し替えました。
- **日付の集計がタイムゾーンによってずれる可能性**: ダッシュボードの「今月」判定が `new Date("2026-08-05")` のようにDateオブジェクトを介して年月を取り出していたため、ブラウザのタイムゾーンがJST（UTC+9）より西側（例: 米国など）だと日付が1日ずれて集計される可能性がありました。日付文字列から直接年月を取り出す形に変更し、タイムゾーンの影響を受けないようにしました。CSV出力のファイル名の日付も同様に修正しています。

### 11. 未定リストを拠点→担当者でグループ表示、操作ヒントの注釈を削除
- 未定リストは、これまで登録順のフラットな一覧でしたが、既定で「拠点 → 担当者」の2段階でグループ化して表示するようにしました（拠点は 岐阜/大垣/多治見/高山/AAA/デモカー/その他 の順、担当者はグループ内で登場順）。拠点・担当者は見出しに出るので、カード自体からは重複する表示（拠点バッジ・SALES表記）を外し、その分カードをコンパクトにしています。
- 「※カードをカレンダーへドラッグ＆ドロップ」「※カードをタップして登録日を確定」という操作ヒントの注釈は削除しました。

### 12. 未定リストを狭めてカレンダーを広げる
未定リストの列幅が画面幅の1/4（`lg:w-1/4`）で、広い画面では中身に対して余白が目立っていました。固定幅 `lg:w-[272px]` に変更し、カレンダー側は残り幅いっぱいに広がる `lg:flex-1` に変更しました（以前は3/4固定でした）。未定リストをOFFにした時の「カレンダー全幅」表示は変更していません。

### 13. UI全面リニューアル（縦スクロールカレンダー・コントロールパネル・マイページ・設定）
プレビュー(Artifact)での多数回のレビューを経て確定したデザインを、実際のアプリに反映しました。主な変更点は以下の通りです。

- **カレンダーを月グリッドから縦スクロールの日付リストに変更**。1日=1行で、登録内容は行内に横並びで表示します。行の高さは登録の有無に関わらず均一です。開いた瞬間に「今日」の行が一番上に来るよう自動スクロールします。
- **土日祝の色分け**：土曜＝青、日曜＝ピンク、祝日＝緑の背景で行全体を色分けします。祝日は固定日＋ハッピーマンデー＋春分/秋分の計算に加え、振替休日・国民の休日も自動判定します（過去の五輪特例年などの例外までは対応していません）。
- **登録カードは固定サイズ**（顧客名は文字数に関わらずカードサイズが変わりません）。1行目に顧客名(太字)＋登録方法、2行目に担当者＋モデルを表示します。
- **未定リストは右側のオーバーレイパネル**に変更（カレンダー幅を圧迫しません）。開閉可能・内部スクロール対応で、月切替ボタンと重ならないよう表示範囲をカレンダー一覧部分に限定しています。
- **集計ヘッダー**：総登録台数・OSS登録台数・OSS登録率のタイル＋拠点別タイルを表示。
- **左側にコントロールパネル**（ナビゲーション）を追加し、「カレンダー」「マイページ」「設定」を切り替えられるようにしました。上部中央のアイコンは設定ページから変更できます（権限者のみ）。
- **マイページ**を新設。ログイン中のGoogleアカウントと「マイページ連携設定」（後述）で紐づけたフルネームをもとに、自分が担当者になっている登録だけを表示します。月切替、月ごとのKPI（受注台数/登録済み/未定/OSS登録率）、顧客名・拠点・登録方法での絞り込み、車検証PDFの閲覧ボタンに対応しています。まだ紐づけされていないアカウントでは、その旨を案内する画面が表示されます。
- **設定ページ**を新設。
  - **テーマ設定**（全ユーザー利用可）：アクセントカラーを8色から選べます。選択は「保存」を押すまで確定しません。保存内容は`PropertiesService.getUserProperties()`でGoogleアカウントごとに保存され、そのアカウントでアクセスしたときだけ反映されます。
  - **サイドパネルアイコン設定**（権限者のみ）：左上に表示するアイコンを選べます。全ユーザー共通の設定として`PropertiesService.getScriptProperties()`に保存されます。
  - **マイページ連携設定**（権限者のみ）：Googleアカウントとフルネームの対応表を管理します。**編集権限（`EDITOR_EMAILS`）とは別の設定**で、ここに登録しても編集権限は付与されません。専用シート `settings_マイページ連携` に保存されます。担当者欄の入力とここに登録するフルネームの表記（全角/半角、旧姓、ニックネームなど）が一致しないと、そのユーザーのマイページには何も表示されないため、表記を揃えて運用してください。
- **リスト表示・CSV出力・検索/絞り込み**など、既存の機能はそのまま維持しています（登録編集モーダル・ドラッグ&ドロップは下記14.の変更で廃止しました）。
- **PC画面の高さにぴったり収まるレイアウト**に変更し、ページ全体はスクロールしません（カレンダー一覧・未定リスト・マイページ一覧など中身が多い箇所だけが内部スクロールします）。860px未満の画面幅では、この固定レイアウトを解除し通常のスクロールに切り替えます（縦スクロールカレンダー自体はそのまま使えますが、既存のスマホ専用タブ切り替えUIほど作り込んではいないため、実機での見え方は別途確認をおすすめします）。

### 14. アプリを閲覧専用に変更し、登録・編集はスプレッドシート直接編集に一本化
これまではアプリ画面からも登録の追加・編集・ドラッグ&ドロップでの日付確定ができましたが、運用をスプレッドシート中心に統一するため、アプリ側からの書き込みをすべて廃止しました。

- **アプリからの新規登録・編集・ドラッグ&ドロップを廃止**：カレンダーの「＋新規登録」ボタン、空の日付行の「登録を追加」、未定リストからカレンダーへのドラッグ&ドロップはすべて削除しました。カード・行をクリックすると開く画面も、全項目readonlyの「登録内容」詳細表示のみになります（保存ボタンはありません）。`Code.gs` の `saveRecord` 関数自体も削除しました。
- **スプレッドシートに直接入力する運用に変更**：`db_登録データ_YYYY_M月` シートを直接編集して登録します。新しい月のシートは、GASエディタの関数選択メニューから `createNextMonthSheet` を実行すると、見出し・入力しやすくするための注釈・プルダウンつきで自動作成されます（既存シートに同じ書式を当て直したいときは `refreshAllSheetTemplates` を実行してください）。
- **各列に入力ガイドの注釈(コメント)を追加**：ヘッダーセルにマウスを乗せると、その列に何を入力すればよいかの説明が出ます。「ステータス」「拠点」「OSS区分」はプルダウンで選択する入力規則も設定しています。
- **ステータスの選択肢を変更**：「登録日未定 / 登録日確定 / 書類準備中 / 書類完了 / 登録完了」の5択から、次の3択に変更しました。
  - 登録書類到着待
  - 登録書類到着済
  - 登録予定日確定（これだけがカレンダーに日付付きで表示され、それ以外は未定リストに表示されます）

  **⚠ 既存データの移行が必要です**：これまでのシートには旧ステータス名（「登録日確定」など）が入力済みのはずです。アプリ側は旧「登録日確定」も引き続き確定扱いとして読み替えるようにしているため、当面はカレンダー表示は壊れませんが、プルダウンの入力規則は新しい3択でしか通らないため、既存セルは「無効な値」として警告表示されます。落ち着いたタイミングで、既存の「登録日確定」を「登録予定日確定」に一括置換（Google スプレッドシートの「検索と置換」機能）することをおすすめします。「登録日未定」「書類準備中」「書類完了」「登録完了」は、いずれもアプリからは「未確定（未定リスト行き）」として扱われるため、実害はありませんが、こちらも新しい3択に整理していただくのが理想的です。
- **スプレッドシートのID列を廃止**：ID列はアプリからの編集・ドラッグ&ドロップの対象を特定するためだけに使っていましたが、その機能自体がなくなったため削除しました。新規シートには含まれません。既存シートに残っているID列は削除しても問題ありませんが、残したままでもアプリの動作に影響はありません（アプリは列名で読み込むため、余分な列は無視されます）。
- **同姓同名の重複チェックをアプリからスプレッドシートへ移動**：アプリ側の重複警告アイコンは廃止しました（同月に同名で2台登録される正当なケースがあるため、アプリ側ではエラー扱いしません）。代わりに、各シートの「顧客名」列に条件付き書式を設定し、同じシート内（＝同じ月）に完全に同じ名前が2件以上あるセルは薄いオレンジ色で自動的に強調表示されます。
- **サイドパネルアイコンをプリセットのアイコン選択から画像アップロード方式に変更**：設定ページからアイコン画像をアップロードできます。画像はGoogle Driveの指定フォルダに保存し、表示側はDriveのサムネイル配信を利用して常に一定サイズの円形アイコンとして表示するため、大きな画像をアップロードしても見た目のサイズは自動的に整います。**保存先のDriveフォルダIDは設定ページ（サイドパネルアイコン設定カード内）から入力・保存できます**。コードの編集は不要です（未設定のままアップロードするとエラーメッセージが表示されます）。
- **未定リスト(オーバーレイパネル)の横幅を縮小**：カレンダー側のカード幅(178px)にあわせて284px→216pxに縮め、開いているときもカレンダーがより広く見えるようにしました。
- **アイコン保存先のDriveフォルダIDを設定ページから指定可能に変更**：これまで`Code.gs`内の定数として直接コード編集が必要でしたが、`getSideIconFolderId`/`saveSideIconFolderId`を追加し、`PropertiesService`（スクリプトプロパティ）で保存するようにしました。設定ページの「サイドパネルアイコン設定」カードにフォルダID入力欄を追加し、権限者はコードを触らずに保存先を設定・変更できます。
- **カレンダー1日あたりの表示件数と余白を調整**：横方向の空きスペースを活かせるよう、1日に表示するカードの上限を3件→8件に拡大し、カードが折り返す際の隙間も詰めて、空白や無駄なスクロールを減らしました。

### 15. 新車・中古車の2本立て化、カレンダーの横スライド表示、お知らせ／カレンダー設定、担当者マスタの拡充

- **カレンダー9件目以降は横スライド表示に変更**：1日の登録件数が9件以上になる日は、これまでの「+N件」タップ→モーダル一覧ではなく、その日の行を左右にスライドして続きを見る方式に変更しました。日付列（左端）はスライドしても位置が固定されたままです。9件目以降がある日は、カード列の右端に丸いスライド案内アイコン（›）が表示され、タップまたは横スクロールで続きを確認できます。
- **お知らせ機能を追加**：設定ページに「お知らせ設定」カード（権限者のみ編集可）を追加しました。登録したお知らせは、画面最上部の全幅バナーに常時表示されます（閉じるボタンで非表示にできますが、次回アクセス時は再度表示されます）。保存は`PropertiesService.getScriptProperties()`に行われ、全ユーザー共通です。
- **カレンダー設定（日付ごとの案内）を追加**：設定ページに「カレンダー設定」カード（権限者のみ）を追加しました。車両区分・年・月を選んで、その月に表示する案内（書類提出締切日など）を日付ごとに最大10件まで登録できます（後述16.のとおり、案内のある日はカレンダー上に直接テキストで表示されます）。
- **新車・中古車の2本立てカレンダーに対応**：左のコントロールパネルの「カレンダー」の下に「新車」「中古車」の切り替えが追加され、それぞれ独立したカレンダー・未定リスト・リスト表示・マイページ集計を確認できます。
  - スプレッドシートは車両区分ごとに完全に別のシート群です。新車は従来どおり `db_登録データ_YYYY_M月` / `db_登録データ_未定`、中古車は新たに `db_登録データ_中古_YYYY_M月` / `db_登録データ_中古_未定` を使用します（新車側の既存シート名・データはそのまま変更していません）。
  - 中古車カレンダーの「拠点別登録台数」タイルからは「デモカー」を除外しています（新車側は従来どおり表示されます）。
  - 車検証PDFの自動リンクも車両区分ごとに独立しています。設定ページに新設した「車検証ファイル設定」カード（権限者のみ）から、新車・中古車それぞれの車検証保管フォルダのDrive IDを、コードを編集せずに登録できます。指定したフォルダ配下は階層の深さに関わらず再帰的に探索し、「2026.09」「2026.9」のどちらの表記の年.月フォルダにも対応して、顧客名が一致するPDFを見つけます。
  - PDF自動リンクの定期実行トリガーの関数名が `autoLinkVehicleInspectionPDF` から `autoLinkVehicleInspectionPDFNewCar` / `autoLinkVehicleInspectionPDFUsedCar` に変わりました。旧トリガーが設定済みの場合はGASエディタの「トリガー」画面から削除し、`setupPdfLinkTrigger` を再実行して新しいトリガー（新車・中古車の2本）を設置し直してください。
  - `createNextMonthSheet()`（新車用）に加えて `createNextMonthSheetUsedCar()`（中古車用）を追加しました。`refreshAllSheetTemplates()` は新車・中古車どちらのシートにもまとめてテンプレートを当て直します。
- **マイページから拠点の絞り込みを削除**、代わりに**ステータス列を追加**：マイページの検索条件から「拠点」の選択を廃止しました（一覧の各行には引き続き拠点バッジが表示されます）。かわりに一覧に「ステータス」列を追加し、スプレッドシートに入力した値（登録書類到着待／登録書類到着済／登録予定日確定）がそのまま確認できます。あわせて、一覧の「車検証」欄の「未アップロード」表記が折り返さないよう幅と`white-space`を調整しました。
- **「マイページ連携設定」を「担当者マスタ」に改称し、拠点の紐づけと保存ボタンを追加**：設定ページのこのカードは「担当者マスタ」という名称になりました。氏名・Googleアカウントに加えて「拠点」も登録できます（登録データの拠点集計とは独立した、担当者マスタ上の管理用の拠点情報です）。アプリからの登録・削除はこれまで即時保存でしたが、テーマ設定などと同様に、変更内容をその場で確定させず「保存」ボタンを押すまでは未保存として扱うように変更しました。また、アプリから直接編集できる項目ではあるものの、権限者アカウント自身の扱いを明確にするため、一覧を「権限者ログインアカウント」と「そのほかのアカウント」の2グループに分けて表示するようにしました（`EDITOR_EMAILS`と照合して自動判定）。
- **スプレッドシート側で担当者マスタ未登録の警告表示を追加**：各シートの「担当者」列に条件付き書式を設定し、担当者マスタ（`settings_マイページ連携`シート）に登録されていない氏名が入力されたセルは薄い赤で強調表示されるようにしました（`applySheetGuidance_`内の`担当者`列チェックとして、既存の「顧客名」重複チェックと同様の仕組みで追加）。

### 16. スプレッドシートのカスタムメニュー、マイページの表示統合、通知UIの改善、担当者マスタと初期カレンダーの連動

- **スプレッドシート画面に「シート設定」カスタムメニューを追加**：スプレッドシートを開くと、メニューバーの「ヘルプ」の横に「シート設定」メニューが追加されます（`onOpen()`トリガーによる標準的なGASカスタムメニュー）。新車・中古車それぞれの「翌月のシートを作成」、全シートへのテンプレート再適用、PDF自動リンクの定期実行設定を、GASエディタを開かずスプレッドシート上から直接実行できます。
- **マイページを新車・中古車の合算表示に変更**：これまでカレンダー側で選択中の車両区分のみを表示していましたが、マイページは常に新車・中古車の両方のデータをまとめて表示するようにしました（`getAllRegistrationData`で両車両区分をまとめて取得）。判別できるよう一覧に「区分」列（新車/中古車）を追加しています。
- **マイページの列を均等幅に整理し、表示順を変更**：一覧の各列をすべて均等なflex幅にして隙間のばらつきをなくし、表示順を「日付・区分・顧客名・モデル・登録方法・ステータス・車検証」に変更しました。「拠点」列は表示から外しています（検索条件の拠点絞り込みは前回のアップデートで既に廃止済みです）。
- **未定リストの開閉ボタンをカレンダーカードの外に移動**：これまでカレンダーカード右端に縦書きで重なって表示していたボタンを、カレンダーカードの外側に独立したボタンとして配置し直しました（配置は後述17.でさらにカレンダーの右側に変更しています）。未定件数のバッジも表示されます。
- **カレンダー案内の表示方法を、アイコン+モーダルから該当日の行内表示に変更**：これまでは案内のある日の日付バッジに小さなアイコンが付き、タップするとモーダルで内容を確認する方式でしたが、内容が分かりにくいという声を受けて、案内のあるその日の行だけ高さを広げ、案内テキストをカレンダー上に直接表示するようにしました（該当日の行に淡いアンバー色の帯を表示）。
- **ヘッダーのお知らせバナーを自動スクロール表示に変更**：これまでは横スクロールバーで手動で見る方式でしたが、左から右へ自動的に流れるマーケー(ティッカー)表示に変更しました（`prefers-reduced-motion`設定時はアニメーションを停止します）。
- **担当者マスタに「担当車両区分」を追加し、アプリを開いた際の初期カレンダーと連動**：担当者マスタの各行に「担当車両区分」（新車/中古車/未設定）を設定できるようになりました。ログインしたGoogleアカウントがこの区分に紐づいている場合、アプリを開いた瞬間からその車両区分のカレンダーが表示されます（未設定の場合は従来どおり新車が既定です）。また、アプリを開いた直後は車両区分に関わらず未定リストは閉じた状態になるよう変更しました。

### 17. お知らせの新車/中古車分離、入力中のスクロール位置維持、横スライドUIの改善、細かなUI磨き込み

- **お知らせを新車・中古車で別々に設定可能に変更**：これまで1つだった「お知らせ」を車両区分ごとに独立させました（`getAnnouncements`/`saveAnnouncements`に車両区分の引数を追加）。設定ページの「お知らせ設定」カードで編集対象の車両区分を切り替えられ、ヘッダーのバナーには現在表示中のカレンダーの車両区分に対応するお知らせだけが表示されます。
- **入力中にスクロール位置が先頭へ戻ってしまう不具合を修正**：リスト表示の検索・マイページの検索で、1文字入力するたびに結果欄を再構築する際、その結果欄自身のスクロール位置が先頭にリセットされていました。再構築の前後でスクロール位置を保持するように修正し、下の方までスクロールした状態で検索文字を入力しても位置が飛ばなくなりました。
- **カレンダーの横スライドで、先端まで到達したら矢印を非表示に変更**：1日の登録が9件以上あり横スライドできる日で、スクロールを右端まで進めると「まだ続きがある」ことを示す矢印アイコンが自動的に消えるようにしました（スクロール位置を監視し、末尾に達したかどうかで表示を切り替えます）。
- **未定リストの開閉ボタンをカレンダーの右側に再配置**：直前のアップデートでカレンダーカードの上に置いていたボタンを、カレンダー本体の右側に縦長のボタンとして配置し直しました。
- **数字の「0」と「8」を区別しやすく調整**：日付・件数などの数字表示（JetBrains Mono使用箇所）に、ゼロに斜線が入る`font-variant-numeric: slashed-zero`を適用しました。
- **UI全体の形状・配色を見直し**：角丸を大きく使いすぎていたタブ・ボタン・カードなどの形状を、全体的により角ばった(四角に近い)形状に統一しました（例：車両区分バッジを完全な楕円形から角丸6pxのバッジに変更）。あわせて、タブ・サイドナビ・ボタン類にホバー時の視覚フィードバックや、選択中であることがわかりやすいインジケーター（サイドナビのアクセントバーなど）を追加し、状態がひと目で分かるようにしました。

### 18. 未定リストボタンの再配置、中古車の拠点タイル幅調整、コントロールパネルの階層線

- **未定リストの開閉ボタンをカレンダーヘッダー行に統合**：カレンダー外の独立した縦長ボタンだったものを、カレンダー/リストのタブや今日・前月・次月ボタンと同じ行（右端）に配置し直しました。トリガーの位置と、開いたときに実際にパネルが表示される位置（カレンダー右側）が近くなり、操作の見通しがよくなりました（リスト表示時はこのボタン自体を表示しません）。
- **中古車カレンダーの拠点別タイルの余白を解消**：拠点別登録台数のタイル行が常に7列固定だったため、中古車表示（デモカーを除いた6拠点）のときに1列分の空白ができていました。タイルの列数を表示件数に応じて動的に変える（`grid-template-columns`を拠点数で算出）ようにし、空きスペースが出ないようにしました。
- **コントロールパネルに「カレンダー→新車/中古車」の階層線を追加**：左のコントロールパネルで、「カレンダー」ナビの下にぶら下がる「新車」「中古車」の選択項目が、その子要素であることが視覚的にわかるよう、ツリー表示のような分岐線（縦線＋各項目への横の接続線）を追加しました。選択中の項目は接続線もアクセントカラーになります（860px未満の横並びレイアウトでは、この装飾線は非表示にしています）。

### 19. テーマ設定の適用範囲を拡大、書体をNoto Sans JPに統一、企業向けソフトの要素を反映

- **テーマ(アクセントカラー)の適用範囲を拡大**：これまでアクセントカラーは一部のボタンやアイコンにしか反映されていませんでしたが、`THEMES`の各テーマに`panelBg`（コントロールパネル全体に敷く、ごく薄いテーマ色の下地）を追加し、左のコントロールパネル全体がテーマカラーで色づくようにしました。あわせて、カレンダーの「今日」「前月」「次月」ボタンのホバー時の配色、登録内容・お知らせなどのモーダル上部の縁取りにもテーマカラーを反映し、テーマ変更の効果が画面のより広い範囲に及ぶようにしました。
- **書体をNoto Sans JPに統一**：これまで欧文（数字・アルファベット）は`Plus Jakarta Sans`、日本語は`Noto Sans JP`と、文字ごとに異なる書体が使われており、これが読みにくさの一因になっていました。すべてのUI文字を`Noto Sans JP`に統一し、和文・欧文が混在する表示（日付・氏名・車種など）でも字形の統一感が出るようにしました（日付や件数などの数値表示は、識別性を優先して引き続き`JetBrains Mono`等幅フォントのままです）。
- **大手企業向けソフトの視覚パターンを部分的に反映**：
  - Microsoft 365 (Fluent UI) 系：左ナビの選択項目に「左端の縦アクセントバー」で現在地を示す表現
  - Google Workspace系：ブランドカラーで薄く色づいたサイドパネルと、白背景のコンテンツ領域を分ける構成
  - Salesforce Lightning系：登録カード左端の拠点カラーバー、ステータスをピル(バッジ)で表示するパターン
  - freee・マネーフォワードなど国内業務SaaS系：Noto Sans JPを基調とした和文タイポグラフィ、単色アクセント＋白背景中心の落ち着いた配色
  - モーダル上部の細いアクセント色の縁取り（ServiceNowやSAP Fioriなど、業務システムのダイアログでよく使われる表現）

### 20. コントロールパネルにサマリーを追加（旧デザインへの巻き戻し後に再適用）

この後、「配色システム・サイドバー・タイポグラフィ・カード表現を一括刷新（重厚感と視認性を強化）」という濃色サイドバー・線中心のカード表現への全面的な作り直しを一度行いましたが、見比べた結果「元のUI（本項目より上の18.までの状態）の方が良い」というご判断により、UI部分をこの時点の見た目まで巻き戻しました。あわせて、巻き戻し後も次のサマリー機能だけは引き続き使えるように、この時点の（薄い色つきパネルの）デザインに合わせて作り直して再適用しています。

- **コントロールパネルに「〇月 サマリー」ブロックを追加**：ナビゲーションと権限者情報の間に、現在選択中の車両区分・表示中の年月を対象とした「総登録台数」「OSS登録率」「未定」の3項目を表示します（`cpSummaryHTML()`）。カレンダー・マイページ・設定のどのページを開いていても確認できます。数値は`--accent-ink`で、カードは白背景＋淡い影で表示し、このREADMEの13.〜19.までのデザイン（明るい配色・柔らかい影）と統一しています。
- **860px未満の横並びレイアウトでは非表示**：表示スペースの都合上、このブロックのみスマホ表示では省略しています（カレンダーページ本体のKPIタイルは従来通り表示されます）。

### 21. 未定リストの月次スコープ化・ヘッダー整理・CSV出力の権限制御・スマホ表示の改善

- **未定リストを表示中の月のスプレッドシートに応じて絞り込み**：これまで「未定」は車両区分内の全月シートを横断して表示していましたが、`getRegistrationData(carType)`（`Code.gs`）でシート名（例：`db_登録データ_2026_9月`）から年・月を`parseSheetMonth_()`で抽出し、各レコードに`_sheetYear`/`_sheetMonth`を付与するようにしました。フロント側の`pendingRecords()`（`Index.html`）はこれを見て、月別シート由来のレコードは表示中の年月に一致するものだけに絞り込み、月に紐づかない専用の「未定」シート（`db_登録データ_未定`など）のレコードは従来通り常に表示します。カレンダーの月送りに合わせて、サイドバー・パネルの未定リストの中身も切り替わります（マイページの未定表示は元々別ロジックのため対象外です）。
- **ヘッダー内「拠点別登録台数」のラベル文字のみ削除**：拠点別タイルの行自体は従来通り`heroSummaryHTML()`内に表示したまま、見出しラベル「拠点別登録台数」の文字だけを取り除きました（数値タイルは以前と同じ位置に表示されます）。
- **コントロールパネル内のサマリーを削除**：20.で追加した「〇月 サマリー」ブロック（`cpSummaryHTML()`と関連CSS）をコントロールパネルから完全に削除しました。
- **CSV出力を権限者限定に変更**：リスト表示の「CSV出力」ボタンは`IS_EDITOR`が真のときのみ描画するようにし、非表示中は関連のクリックイベントも設定されません。念のため出力処理本体の`downloadCSV()`の先頭にも`IS_EDITOR`チェックを追加し、ボタンを介さない実行経路からもCSVが出力されないようにしています。
- **スマホビュー専用の「ダッシュボード」ページを新設**：860px未満の画面ではコントロールパネルに「ダッシュボード」ナビ項目が現れ（`dashboardPageHTML()`）、総登録台数・OSS登録率などのKPI、拠点別登録台数、未定リストをひとつの縦スクロールページにまとめて確認できます。あわせて、カレンダーページ本体のKPIサマリー（`heroSummaryHTML()`）は`.calendar-hero`でラップし、スマホ表示時のみ非表示にしています（ダッシュボード側では引き続き表示されます）。PC表示（860px以上）ではこのナビ項目自体を非表示にしており、レイアウト・挙動とも変更はありません。
- **（訂正）拠点別タイルの表示自体は削除しないよう修正**：上記「ヘッダー内『拠点別登録台数』を削除」を一度、タイル行ごと取り除く形で実装しましたが、「見出し文字だけを消してほしかった。実際の数値は以前と同じ箇所に表示してほしい」というご指摘を受け、`heroSummaryHTML()`に拠点別タイルの行を戻し、ラベル文字のみ削除する形に修正しました。これに伴い、拠点別タイルの描画をダッシュボード用に切り出していた`branchSummaryHTML()`は不要になったため削除し、ダッシュボードも`heroSummaryHTML()`のみで拠点別タイルを表示するようにしています。
- **リスト表示・マイページのスマホ表示を改善**：リスト表示のテーブルは`.list-table-wrap`で囲んで横スクロール可能にし、狭い画面でも列が潰れず読めるようにしました。マイページの行（`.mp-row`）は7項目の横一列表示から、スマホ表示時のみ`flex-wrap`による2カラムの折り返し表示に変更し、見出し行（`.mp-thead-row`）は折り返しと噛み合わないため非表示にしています。いずれもPC表示（860px以上）の見た目・挙動は変更していません。

### 22. スマホビューをアプリ風レイアウトに全面刷新

「スマホビューが操作しづらく視認性が悪い」というご指摘を受け、860px未満の画面のみ、ネイティブアプリのような操作感になるようレイアウトを全面的に作り直しました。PC表示（860px以上）は`@media (max-width: 860px)`の外側のCSSを変更していないため、見た目・挙動とも一切変更ありません。

- **ボトムタブバーを新設**：画面下部に固定の「カレンダー／ダッシュボード／マイページ／設定」タブバー（`bottomTabBarHTML()`）を追加しました。既存のコントロールパネルのページナビ用クリックハンドラ（`.cp-nav-item`への委譲イベント）をそのまま利用しているため、JS側の新規配線なしに動作します。これに伴い、21.でコントロールパネルの一部に間借りしていた「ダッシュボード」ナビ項目は廃止し、ボトムタブバー側に一本化しました（PC表示にはボトムタブバーは表示されないため、ダッシュボードは引き続きスマホ専用です）。
- **上部バーをロゴ＋車両区分の切り替えだけのアプリ風ヘッダーに簡略化**：ページナビはボトムタブバーに移したため、コントロールパネル（スマホ表示時）は「AU NEW CAR」ロゴと新車/中古車の切り替えのみを表示する、画面上部に固定（sticky）されたコンパクトなバーになりました。ページ送りのたびに再度スクロールし直す必要がありません。
- **カレンダーヘッダーを縦積みに再構成**：拠点/リスト切り替え・年月・移動ボタン・未定リストボタンが1行に詰め込まれて操作しづらかった問題を、`.calhead-row`/`.calhead-group`のクラスに置き換えたうえで、スマホ表示時のみ縦に積む形に変更し、未定リストボタンは独立した全幅ボタンにしました。
- **未定リストをサイド固定パネルからボトムシートに変更**：スマホ表示では未定リストパネル（`.cal-overlay-panel`）が画面下からせり上がるボトムシート（丸みを帯びた上端、つまみ表示付き）になり、内容は従来通り拠点・担当者ごとにグルーピングされたカード一覧です。
- **リスト表示をテーブルからカード一覧に変更**：スマホ表示では表（`.list-table-wrap`）を隠し、代わりに1件ずつカード化した一覧（`.list-card-wrap`）を表示するようにしました。顧客名・日付・方法・拠点・担当者・車種・備考が縦に読みやすく並びます（PC表示は従来通り表形式です）。
- **マイページの行をCSS Gridによる2列カードに変更**：21.で行った`flex-wrap`による折り返し表示はなお窮屈だったため、`.mp-row`をスマホ表示時のみ`grid-template-areas`で「日付/ステータス」「顧客名」「モデル」「区分/登録方法」「車検証ボタン」の5段カードに再構成しました。各列に`.mp-col-date`等のクラスを追加し、DOM順を変えずに表示順を制御しています。
- **拠点別タイルを3列表示に**：ダッシュボードの拠点別タイル（`.kpi-row-bottom`）は、スマホ表示時のみ`grid-template-columns`を3列に上書きし（インラインstyleで指定された7列/6列を`!important`で上書き）、狭い画面でも数値が読み取れるようにしました。
- **アカウント情報を設定ページに再配置**：スマホ表示ではコントロールパネル下部のアカウント情報（`.cp-footer`）が非表示になるため、代わりに設定ページの先頭に同内容のカード（`.mobile-account-card`）を表示するようにしました。
- **タップ操作のしやすさを改善**：入力欄・ボタン・タブ・テーマスウォッチなど主要な操作要素にスマホ表示時のみ`min-height: 40px`を指定し、指で押しやすい大きさを確保しました。

### 23. スマホ表示のKPI表示・ダッシュボード構成・ボタンサイズを調整

22.のアプリ風レイアウトに対して、「文字の折り返し・枠内の空白をなくしてほしい」「総登録台数や拠点別登録台数の見せ方をよりスマホらしい形式に」「ダッシュボード内の総登録台数・OSS登録台数・OSS登録率、および各拠点の数値をそれぞれ一列に並べてほしい」「未定リストはカレンダータブ内のみにしてほしい」「ボタンの無駄な余白を詰めて、なるべく小さくしてほしい（押しにくくない範囲で）」というご指摘を受け、スマホ表示のみ以下の通り調整しました。PC表示は変更していません。

- **KPI表示を「統計ストリップ」形式に変更**：`heroSummaryHTML()`が生成する総登録台数・OSS登録台数・OSS登録率、および拠点別タイルは、22.まではそれぞれ個別の箱（タイル）が並ぶグリッド形式でしたが、スマホ表示時のみ、1枚のカードを縦の仕切り線で区切る単一行の形式に変更しました（`.kpi-row-top`/`.kpi-row-bottom`を`display:flex`化し、各`.kpi-tile`の背景・影・角丸を透明化して仕切り線のみに）。これにより、総登録台数・OSS登録台数・OSS登録率は常に横一列に並び、拠点別の数値も同じ形式の単一行（項目数が多い場合は横スクロール）で表示されます。ラベル文字は`white-space:nowrap`と省略記号で折り返しを防止し、タイル間の余白も最小限にしています。
- **ダッシュボードから未定リストを削除**：未定リストはカレンダータブ内のボトムシート（22.で実装済み）に一本化し、ダッシュボードページ（`dashboardPageHTML()`）からは`pendingPanelHTML()`の呼び出しを削除しました。ダッシュボードはKPI表示のみのシンプルな構成になっています。
- **ボタン類の余白を詰めてコンパクトに**：22.で追加した`min-height: 40px`の一律指定（かえって余白が目立つ原因になっていました）を廃止し、`.nav-btn`/`.today-btn`/`.tab`/`.action-btn`/`.pending-toggle-btn`/`.settings-input`の縦方向パディングを個別に切り詰めました。PC表示時のサイズより一回り小さくなりますが、タップ操作に支障のない範囲に留めています。

### 24. 横スクロール矢印の一般化、ダッシュボードに新規ウィジェット追加、カレンダー・マイページの表示調整、テーマ設定を2×4グリッドに

引き続きスマホ表示のみを対象に、「横スクロールがある箇所には矢印を付けてわかりやすく」「ダッシュボードの空白部分にスマホ向けの独自機能を追加」「カレンダーの登録カードは横に2件分だけ表示し残りは横スクロール（矢印付き）に」「マイページも横長・列を細く・折り返しなしに」「テーマ設定は2色×4段のグリッドに」というご指摘を受けて調整しました。PC表示は変更していません。

- **横スクロール矢印(`.slide-hint`)を主要な横スクロール箇所すべてに拡張**：これまでカレンダーの登録カード列（`.content-col`）だけに実装されていた「右へスライドできることを示す矢印ボタン」の仕組みを汎用化し（`wireEvents()`内の対象セレクタを`.content-col, .kpi-row-top, .kpi-row-bottom, .day-chart`に拡張）、KPIの統計ストリップや新設の日別チャート（後述）にも同じ矢印を表示するようにしました。矢印は「実際にスクロールしきって先がなくなったら自動的に消える」既存ロジックをそのまま利用しているため、項目数が少なく実際にはあふれない場合は表示されません。あわせて、カレンダーの登録カード列で矢印の表示・非表示を判定していた`entries.length > 8`という決め打ちの閾値（PC表示を前提にした値で、スマホでは全く機能していませんでした）を`entries.length > 1`に見直し、実際の折り返し有無をJS側で正しく検出できるようにしました。矢印が最後のカード/タイルの文字に重ならないよう、各スクロール領域の右側に余白も追加しています。
- **ダッシュボードに「登録方法の内訳」「日別登録件数」ウィジェットを追加(スマホビューオリジナル)**：空きスペースが目立っていたダッシュボード下部に、OSS/紙登録の件数比率を1本のバーで示す`methodBreakdownHTML()`と、当月の日ごとの確定登録件数を横スクロールの棒グラフで示す`dayChartHTML()`（`dailyCountsForMonth()`で日別に集計）を追加しました。いずれもPC表示では导线がなく実質使われないため、`dash-section`系のクラスは特別な出し分けをせずそのまま実装しています。
- **カレンダーの登録カードを「横2件分表示→以降は横スクロール」に変更**：`.entry-card`の幅をスマホ表示時のみ`calc(50vw - 81px)`（画面幅から周囲の固定要素の幅を差し引いて2等分）で指定し、日ごとの登録カードが常に約2件分の幅で表示され、3件目以降は横スクロール（矢印付き）で確認する形にしました。
- **マイページの行を横長の1行レイアウトに変更**：23.まではモバイル表示のみ日付/顧客名/モデル/区分/方法をグリッドで縦に積む5段カード形式でしたが、「横長にして列を細く、折り返しはやめてほしい」というご指摘を受け、PC表示に近い横一列の行に戻しました。日付・区分・登録方法チップは幅を切り詰めて縮小し、車検証ボタンはラベル文字を隠してアイコンのみに（`.mp-pdf-label`をCSSで非表示）、顧客名・モデルだけ可変幅にして収まりきらない分は省略記号で省くようにしています。情報量とのバランスを取るため、ステータスピルの列はスマホ表示でのみ非表示にしました（タップすれば詳細モーダルで確認できます）。
- **テーマ設定を2色×4段のグリッドに変更**：`.theme-swatches`をスマホ表示時のみ`display:flex`の折り返しから`grid-template-columns: repeat(2, 1fr)`の2列グリッドに変更し、8種類のテーマが常に2列×4行で整列するようにしました。

### 25. ダッシュボードページを廃止しカレンダーに統合、未定リストをリスト表示にも追加、カレンダー/リスト切り替えをアイコン化

「ダッシュボードで追加した内容を削除してPCビュー同様にカレンダーに統合してほしい」「未定リストはリストの横に表示してほしい」「カレンダー/リスト切り替えボタンは文言ではなくアイコンにしてほしい」というご指摘を受けて調整しました。

- **ダッシュボードページを廃止**：22.〜24.で追加した専用ページ（`dashboardPageHTML()`）と、そこにのみ搭載していた「登録方法の内訳」バー・「日別登録件数」チャート（`methodBreakdownHTML()`/`dayChartHTML()`/`dailyCountsForMonth()`と関連CSS）を削除しました。ボトムタブバーからも「ダッシュボード」項目を外し、カレンダー/マイページ/設定の3項目に戻しています。KPIサマリー（総登録台数・OSS登録台数・OSS登録率・拠点別台数）は、22.でスマホ表示時のみ非表示にしていた`.calendar-hero`の表示制御をやめ、PC表示と同じくカレンダータブの上部に常時表示する形に統合しました。
- **未定リストをリスト表示（カード一覧）でも利用可能に**：これまで「未定リスト」の切り替えボタンとボトムシートは、カレンダー（グリッド）表示のときだけ表示され、リスト表示に切り替えると使えなくなっていました。`calendarHeaderHTML()`の未定リストボタンを両モード共通で常に表示するように変更し、リスト表示でもヘッダー内の「リスト」タブの横から未定リストを開けるようにしました（中身は従来通り拠点・担当者ごとのカード一覧のボトムシートです）。この変更はPC表示にも及び、これまでPC版のリスト表示にはなかった未定リストへのアクセスが、カレンダー表示と同じボタンで可能になりました。
- **カレンダー/リスト切り替えをアイコンボタンに変更（スマホのみ）**：タブに`fa-calendar-days`/`fa-list-ul`のアイコンを追加した上で、スマホ表示時のみ文言（`.tab-label`）を隠してアイコンのみの表示にしました。PC表示は従来通りアイコンなしの文言表示のままです。

### 26. スマホ表示のレイアウト固定・折り返し/はみ出し修正・未定リストをPC同様の右サイドパネルに・月移動の書式変更

「マイページ内の折り返し・はみ出しを直してほしい」「カレンダータブはお知らせ〜未定リストまでを固定し、カレンダー内の情報のみスクロールできるようにしてほしい」「未定リストボタンはカレンダー/リスト切り替えの横に置き、PCビューのように右サイドから（グレーの背景で）確認できるようにしてほしい」「月移動を『◀ 9月2026年 ▶』の書式にし、『今日』ボタンは不要」「マイページも絞り込み機能より上を固定してほしい」というご指摘を受けて、スマホ表示のみ以下の通り修正しました。

- **画面シェルを固定し、コンテンツ領域だけがスクロールするPC同様の構造に戻す**：22.でスマホ表示のみ`height: auto; overflow: visible`にして「ページ全体がスクロールし、上部バーだけをsticky」という構造にしていましたが、今回この`.app-shell-outer`/`.app-shell`/`.main-col`/`.page-fill-area`/`.calendar-card`/`.rowscroll-wrap`/`#rowScroll`のスマホ専用上書きを撤去し、PC表示と同じ「`height:100vh`の固定シェル＋`.page-fill-area`(または`.rowscroll-wrap`)だけがオーバーフローしてスクロールする」構造に戻しました。これにより、お知らせバナー・上部バー・KPIサマリー・カレンダーヘッダー（拠点/表示切替・月移動・未定リストボタン）は常に画面上部に固定され、スクロールするのはカレンダーの日付行（またはリストの中身）だけになります。あわせてボトムタブバーも`position: fixed`をやめ、固定シェルの一番下に収まる通常のフレックス項目に変更しました。マイページも同じ仕組みの上に乗っているため、`#mpResults`（絞り込み結果一覧）だけがスクロール領域になり、それより上（アバター・タイトル・月移動・KPI・絞り込みフォーム）は自動的に固定されます。
- **未定リストをPC同様の右サイド固定パネル（グレーの背景）に戻す**：22.でスマホ表示のみ画面下からせり上がる「ボトムシート」に変更していましたが、その専用CSSを削除し、PC表示と同じ`position:absolute`の右サイドパネル＋グレーの半透明背景（`.cal-overlay-backdrop`）に統一しました。あわせて、リスト表示でオーバーレイがヘッダー行（拠点/表示切替・未定リストボタン)まで覆ってしまっていた表示崩れを修正し（`.page-fill-area`とオーバーレイを`.rowscroll-wrap`で包み、ヘッダーの下からパネルが始まるようにした）、これはPC表示のリスト画面にも共通する修正です。
- **未定リストボタンをカレンダー/リスト切り替えの横に移動（スマホのみ）**：`calendarHeaderHTML()`で未定リストボタンを2箇所（PC同様に月移動の並びと、拠点/表示切替の横）に描画し、CSSでどちらを見せるかをスマホ/PCで切り替えるようにしました（`.pending-toggle-mobile-slot`/`.pending-toggle-desktop-slot`）。クリック判定は共通クラス`js-pending-toggle`で一括配線しているため、どちらの位置でも同じように開閉できます。PC表示のボタン位置・挙動は変更していません。
- **月移動を「◀ 9月2026年 ▶」の書式に変更し、「今日」ボタンを非表示に（スマホのみ）**：`flex`の`order`プロパティで月移動ボタンを前後に並べ替え、年の表示にも「年」を付けるようにしました（`.calhead-year-suffix`をスマホ表示時のみ表示）。「今日」ボタンはログイン時に自動で本日の行が最上部に表示される（`scrollTodayToTop()`）ため、スマホ表示でのみ非表示にしました。PC表示の月移動UIと「今日」ボタンは変更していません。
- **マイページの折り返し・はみ出しを修正**：マイページ上部のヘッダー行を`.mp-header-row`/`.mp-header-id`/`.mp-header-nav`のクラスに置き換えてスマホ表示時のみ縦積みにし、説明文（山田太郎さんの受注・登録状況…）は`.mp-subtitle`として幅を明示的に確保した上で省略記号表示にしました。KPIタイル（受注台数/登録済み/未定/OSS登録率）は、Tailwindの4等分グリッドのままだと狭い画面で文字が折り返す/はみ出す原因になっていたため、カレンダーのKPIと同じ「統計ストリップ」形式（`.mp-kpi-row`）に変更し、折り返しなく横一列（あふれる場合は矢印付きで横スクロール）に収まるようにしました。PC表示は元の4等分グリッド・横並びレイアウトのままです。

### 27. PC・スマホ全機能のデバッグ（JSロジック・CSSレイアウトの2チーム体制でレビュー）

「PCビュー、スマホビュー全ての機能、UIをデバッグしてほしい」というご依頼を受け、2つのサブエージェント（JSロジックの正確性レビュー担当、CSS/レイアウトの整合性レビュー担当）による並行レビューと、自分自身によるPlaywrightでのPC/iPhone 13/iPhone SE(320px)実機操作テストを組み合わせてデバッグを行いました。見つかった問題は以下の通り修正しています。

**JSロジックのバグ修正**
- **月末日でカレンダーの月送りが壊れる不具合を修正**：`currentDate`を`new Date()`で初期化した際の「実際の日にち（例:31日）」を保持したまま`setMonth()`していたため、例えば1月31日を表示中に「翌月」を押すと、2月には31日が存在しないためJSの仕様で自動的に3月にずれてしまい、2月が丸ごと読み飛ばされる（逆に3月31日から「前月」を押しても2月に留まれず3月に戻ってしまう）不具合がありました。前月/次月ボタンで年月を切り替える際、日にちを1日に固定して`Date`を作り直すように修正しました（`#btnPrevMonth`/`#btnNextMonth`）。
- **担当者マスタ保存時に「保存しました」のメッセージが表示されない不具合を修正**：保存成功後にメッセージをセットした直後、同じ担当者一覧を再取得する`loadMypageLinksIfNeeded()`を呼んでいたため、その再取得が完了した時点でメッセージが空文字にリセットされ、画面に描画される前に消えてしまっていました。お知らせ設定・カレンダー設定の保存処理と同じパターン（ローカルの状態を更新して即座に`render()`する）に揃えて修正しました。
- **拠点の絞り込みで「デモカー」の扱いが画面ごとに不統一だった点を修正**：拠点別KPIタイルは中古車表示時に「デモカー」を除外する`branchOrderFor(carType)`を使っていましたが、未定リストパネルの拠点並び順とリスト表示の拠点フィルタープルダウンは車両区分を考慮しない`branchOrder`のままでした。両方とも`branchOrderFor(carType)`を使うように統一しつつ、未定リストパネル側は万一「デモカー」拠点の中古車データが実在した場合にも表示から消えてしまわないよう、対象の拠点一覧に無い拠点があれば末尾に追加して表示するフォールバックを入れています。あわせて、車両区分の切り替え時に選択中の拠点フィルターが新しい車両区分では選べない拠点を指していた場合は「すべて」に戻すようにしました。
- **Code.gsのデータ取得失敗を`Logger`に記録**：`getRegistrationData`は取得エラー時に空配列を返す仕様（フロント側はこれを「登録0件」として扱う）はそのままに、実際のエラー内容を`Logger.log`に出力するようにし、本当のエラーと単なる0件を後から区別できるようにしました。

**CSS/レイアウトのバグ修正**
- **設定ページの管理者向けフォームがスマホで横スクロールしていた不具合を修正**：「カレンダー設定」の日付＋案内文入力行と、「担当者マスタ」の氏名・メール・拠点・車両区分の行は、スマホ表示に対応するCSSが一切なく、必要な最小幅を足すと画面幅（320〜390px）を大きく超えて横スクロールが発生し、日付欄や削除ボタンが画面外に押し出されていました。スマホ表示時のみ`flex-wrap`で折り返し・縦積みにし、氏名・メールアドレスは省略記号表示にして解消しました。
- **カレンダーの登録カードの横幅計算がずれていた不具合を修正**：スマホ表示で「横に2件分だけ表示」するための`calc(50vw - 81px)`という計算式が、実際の余白（左右の各種padding・拠点日付欄58px・矢印分の余白34pxなど）を積み上げて再計算すると6px分ずれており、2件表示のつもりが常に約12pxはみ出していました。正しい値（`calc(50vw - 87px)`）に修正しています。
- **横スクロール矢印がカードの文字に重なっていたのをPC表示でも解消**：横スクロール時に表示される矢印ボタン用の余白（`padding-right: 34px`）は26.までの作業でスマホ表示にしか適用しておらず、PC表示では登録カードが3件以上並ぶ日に矢印がカードの文字（OSS/紙のピルなど）に重なっていました。この余白をPC・スマホ共通のベースCSSに統合し、両方で正しく余白が確保されるようにしました。

いずれもPlaywrightでPC・iPhone 13・iPhone SE(320px幅)の3パターンで再現・修正確認を行い、コンソールエラーが発生していないことも確認済みです。

### 28. テーマカラー追加、お知らせ帯のアニメーション修正、お知らせ・カレンダー案内の文字色設定、登録予定日確定時のカレンダー自動反映

**テーマに2色追加**
- テーマ設定に「ネイビー」「テラコッタ」の2色を追加し、選べるテーマを全10色にしました（`THEMES`オブジェクトとCode.gs側の検証用`THEME_KEYS`の両方に追加）。スウォッチは2列グリッドのまま自動的に5行になり、レイアウト崩れがないことをPC・スマホ双方で確認済みです。

**お知らせ帯（バナー）が一列流れきる前に止まって見える不具合を修正**
- 従来は同じお知らせ文を2つ並べて複製し、その複製全体の幅を基準にした`translateX(-50%)`⇔`translateX(0%)`という割合指定でループさせていました。お知らせ文が短いと、複製2つ分の幅自体がバナーの表示枠（`flex:1`で伸びる分だけ幅がある）より狭くなり、実際に動く距離がわずか数十pxしかない状態になって「少し動いてすぐ止まる（ループする）」ように見えていました。
- 表示枠の実測幅（`--announce-track-w`というCSSカスタムプロパティとしてJSから設定）を使い、「表示枠の右端の外側から始まり、お知らせ文の左端が表示枠の左端の外側に出るまで」を必ず1周とするアニメーションに変更しました。これにより、お知らせ文の長さに関わらず必ず画面を一列分横切ってからループするようになります。あわせて、移動距離（表示枠幅＋お知らせ文の幅）に応じて表示秒数を8〜40秒の範囲で自動調整し、長いお知らせと短いお知らせで体感速度が大きく変わらないようにしています。

**お知らせ設定・カレンダー設定に文字色の指定を追加**
- 設定ページの「お知らせ設定」で、お知らせ項目ごとにカラーピッカー（`<input type="color">`）で文字色を指定できるようにしました。指定した色は画面上部のお知らせ帯にそのまま反映されます（未指定の場合は従来通りの白文字）。
- 「カレンダー設定」の日付ごとの案内も同様に、行ごとに文字色を指定できるようにしました。指定した色はカレンダー画面の日付行に表示される案内バナーの文字色に反映されます。
- Code.gs側では、保存時に色の値が`#RRGGBB`形式かどうかを検証し（`isValidHexColor_`）、不正な値は保存しないようにしています。

**登録予定日確定時、担当者のGoogleカレンダーへ自動反映**
- ステータスが「登録予定日確定」になった行について、担当者マスタ（設定ページの「担当者マスタ」）で紐づいているGoogleアカウントのカレンダーに、顧客名・車種（モデル）・OSS区分（登録方法）を自動で反映する機能を追加しました。
- 登録・編集はスプレッドシート側で直接行う運用のため、この反映も「車検証PDF自動リンク」と同じ考え方で、15分間隔の定期実行トリガー（`syncConfirmedRegistrationsToCalendarNewCar` / `syncConfirmedRegistrationsToCalendarUsedCar`）で動作します。スプレッドシートのメニュー「シート設定 > 登録予定日確定時のカレンダー自動反映を設定」（`setupCalendarSyncTrigger`）から一度だけ設定してください。
- 二重登録を防ぐため、各db_登録データシートに「登録カレンダー反映」という列を自動追加し、反映済みの行には作成したカレンダーイベントのIDを記録します（車検証リンク列と同様、自動反映専用の列につき手動編集はしないでください）。
- **事前設定が必要です**：この機能はトリガーを設置したアカウント（Googleカレンダーの標準的な共有機能を使う都合上、通常は管理者アカウント）から各担当者のカレンダーへイベントを作成する仕組みのため、**各担当者は自分のGoogleカレンダーを、トリガー設置アカウントに対して「予定の変更権限」付きで共有しておく必要があります**。共有されていない担当者や、担当者マスタに未登録の担当者名が入力された行はスキップされ、他の行の処理やトリガー自体は止まりません（`appsscript.json`に`https://www.googleapis.com/auth/calendar`のスコープを追加しています）。
- 本番のGoogleカレンダーへの書き込みはサンドボックス環境では実行・確認できないため、ロジック面（対象行の抽出条件、担当者マスタとの突合、二重登録防止の列追加処理）はコードレビューと構文チェックで確認済みですが、実際のカレンダー共有権限まわりの動作は、上記の事前設定を行った上で運用環境で一度ご確認いただくことをおすすめします。

### 29. アプリ全体のフォントをiOS Human Interface Guidelinesに準拠したシステムフォントに変更

「アプリ全体の文字体をiOS Human Interface Design にしてください」というご依頼を受け、Google Fontsから読み込んでいた「Noto Sans JP」（本文）と「JetBrains Mono」（KPI数値・日付・時刻などの数字表示）を廃止し、iOS Human Interface GuidelinesがネイティブUIの標準としているSan Francisco（システムフォント）を使うフォントスタックに切り替えました。

- 新しく`--font-ios`というCSSカスタムプロパティを定義し、`-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", "Hiragino Kaku Gothic ProN", "Hiragino Sans", "Yu Gothic Medium", "Yu Gothic", "Segoe UI", Meiryo, Roboto, sans-serif`というフォントスタックを指定しました。`-apple-system`/`BlinkMacSystemFont`はiOS・macOS・Safari上でSan Francisco（システムフォント）そのものに解決されるキーワードで、iOS Human Interface Guidelinesが推奨する「OS標準フォントをそのまま使う」という考え方に沿っています。San FranciscoはCJK（日本語）グリフを含まないため、AppleデバイスのCJK表示で標準的に使われる「ヒラギノ角ゴシック」をすぐ後ろに続け、それ以外のOS（Windows／Android）でも違和感のないよう各OS標準フォント（Segoe UI・Yu Gothic・Meiryo・Roboto）にフォールバックするようにしています。
- 本文（`body`）だけでなく、KPI数値・日付バッジの数字・未定リストの件数バッジ・マイページの日付チップ・カレンダーヘッダーの月/年表示など、以前「JetBrains Mono」で等幅表示していた数字系の要素もすべて同じ`--font-ios`に統一しました（桁揃えのための`font-variant-numeric: tabular-nums`指定はそのまま残しており、San Francisco自身が持つ等幅数字機能で引き続き桁が揃います）。
- 外部フォント（Google Fonts）の読み込み自体をやめたため、`<head>`内の該当`<link>`タグも削除しました。これによりフォントファイルのダウンロード待ちがなくなり、初回表示がわずかに速くなる副次効果もあります。

### 30. お知らせ・カレンダー案内に部分書式（太字・文字色）を設定できる機能を追加

「お知らせ機能およびカレンダー内のお知らせ機能に書式設定が可能にしてください。文脈内での部分文字の編集（太さ、カラー）などが調整できるようにしてください。」というご依頼を受け、前回（28番）で追加した「項目全体に1色だけ設定できる」方式から、文章の一部分だけを選択して太字・文字色を指定できる方式に作り直しました。

**編集UI**
- 「お知らせ設定」「カレンダー設定」の各項目が、直接クリックして文字を打てる編集領域（`contenteditable`）になりました。マウスやタップで文字の一部分だけを選択してから、ツールバーの<i class="fa-solid fa-bold"></i>ボタンを押すとその部分だけ太字に、カラーピッカーで色を選ぶとその部分だけ文字色が変わります。選択せずに操作した場合は何も起きません。
- 太字と文字色は組み合わせて使えます（同じ部分に両方適用可能）。

**データの持ち方**
- 各項目は`{ text, html }`という形（カレンダー案内は`{ date, text, html }`）で保存されます。`text`は検証用のプレーンテキスト、`html`は実際に太字・色を反映して表示するためのHTML断片です。
- 前回（28番）の「項目全体に1色」の形式（`{ text, color }`）で保存済みのデータも、読み込み時に自動的に新しい形式へ変換して表示されます（`migrateRichItem_`）。

**安全対策（XSS対策）**
- 許可するタグ・属性は`<b>`と、`style="color:#RRGGBB;"`だけを持つ`<span>`のみに固定しています。
- 編集領域への貼り付け（コピー＆ペースト）は常にプレーンテキストとして扱うようにし（`forcePlainTextPaste_`）、外部から書式付きHTMLをそのまま貼り付けても余計なタグが混入しないようにしています。
- 保存時にはCode.gs側でも独自にサニタイズしています（`sanitizeRichText_`）。Apps ScriptのV8ランタイムには本物のHTMLパーサ（DOMParser）が無いため、「まず内容全体をHTMLエスケープしてから、許可した書式の並びだけを実際のタグへ戻す」という方式を採用しました。この方式では、たとえ`<span style="color:#F00;" onclick="...">`のように余計な属性が混ざった値が渡されても、正規表現が完全一致しないため無害なテキストとしてエスケープされたまま残ります。この検証は、ブラウザの画面を経由しない直接呼び出し（`google.script.run`の不正利用）に対しても有効です。
- `<script>`タグの混入、`onerror`/`onclick`等のイベント属性、`javascript:`スキームなど代表的なXSSパターンを実際にブラウザのDOMへ挿入して検証し、いずれも要素化・属性化されず無害なテキストとして表示されることを確認済みです。

### 31. お知らせ帯を常時・中央固定表示に変更、拠点マスタの追加、カレンダー自動反映の原因調査用メニューを追加

**お知らせ帯（バナー）を常時表示・中央固定・大きめの表示に変更**
- 前回（28番）で「一列分スクロールしてから流れる」形にしたお知らせ帯を、ご要望により「ヘッダー内に中央固定で表示し、流れない」形に作り直しました。
- 個人がバナーを閉じられる「×」ボタンを廃止しました。以後、お知らせは権限者が設定ページで消すまで、全員の画面に表示され続けます。
- 枠の上下の余白と文字サイズを大きくしました（`padding: 8px 16px` → `16px 20px`、`font-size: 12px` → `16px`）。文章が長い場合は折り返して複数行になり、枠の高さも自動的に大きくなります。

**拠点マスタを追加（設定ページ「拠点設定」）**
- これまでコード（`Code.gs`の`BRANCH_OPTIONS`、`Index.html`の`branchColors`）に直書きしていた拠点名と色を、設定ページから権限者が自由に追加・削除・変更できるようにしました。カレンダー画面のKPIタイル、リストの絞り込み、担当者マスタの拠点選択など、拠点名を使うすべての箇所がこの設定に連動します。
- ここで登録した拠点名は、スプレッドシート側「拠点」列のプルダウン選択肢にも自動的に使われます。既存のシートに反映するには、スプレッドシートのメニュー「シート設定 &gt; 新車・中古車 全シートにテンプレートを再適用」を実行してください（新しく作成する月のシートには自動的に反映されます）。
- どの拠点にも一致しないデータの受け皿である「その他」は、これまで通り自動的に用意されており、拠点マスタで管理する必要はありません。同様に、中古車のKPIタイルから「デモカー」という名前の拠点だけを除外する仕様（27番までの実装）もそのまま維持しています。
- **今後、拠点名や車検証取得フォルダだけが異なる別店舗向けアプリを複製する際、この対応により拠点名もコード変更なしで設定できるようになったため、複製後のコード修正が実質不要になりました**（スプレッドシートを複製し、拠点マスタ・車検証フォルダ・担当者マスタをそれぞれ設定するだけで展開できます）。

**登録予定日確定時のカレンダー自動反映に、原因調査用の「今すぐ実行」メニューを追加**
- 「スプレッドシート側は登録予定日確定にしたのに、Googleカレンダーに反映されない」という状況を権限者自身で調査できるよう、スプレッドシートのメニューに「シート設定 &gt; 登録予定日確定時のカレンダー反映を今すぐ実行（結果・原因を確認）」を追加しました。実行すると、15分間隔の定期実行を待たずにその場で反映を試み、次の内容をダイアログで表示します。
  - 反映できた件数
  - 「担当者マスタに未登録」でスキップした件数と、どの行のどの担当者名が原因か
  - 「カレンダーにアクセスできない（共有設定を確認）」でスキップした件数と、どのメールアドレスが原因か
  - 定期実行トリガー自体が設定されているかどうか（未設定の場合は、そもそも自動では動きません）
- 反映されない主な原因として、(1) 定期実行トリガーが未設置（メニュー「登録予定日確定時のカレンダー自動反映を設定」を未実行）、(2) 担当者マスタ（設定ページ「担当者マスタ」）に、スプレッドシートの「担当者」列と完全に一致する氏名・Googleアカウントが未登録、(3) 対象の担当者が、トリガーを設置したアカウントに対して自分のGoogleカレンダーを「予定の変更権限」付きで共有していない、の3つが考えられます。上記メニューの実行結果で、この3つのうちどれに該当するかを確認できます。

### 32. 拠点設定を新車・中古車で別々に、お知らせ帯の色をテーマ連動に変更

**拠点設定を新車・中古車で別々に管理できるように変更**
- 前回（31番）で追加した拠点マスタは新車・中古車で共通の1つのリストでしたが、ご要望により新車用・中古車用をそれぞれ別々に登録・編集できるようにしました。設定ページ「拠点設定」に、お知らせ設定と同じ形式の車両区分セレクタを追加し、選んだ車両区分の拠点だけを表示・編集します。
- 保存すると、その車両区分のシート（`db_登録データ_...`／`db_登録データ_中古_...`）の「拠点」列プルダウンだけに反映されます（もう一方の車両区分には影響しません）。
- マイページや担当者マスタなど、車両区分をまたいで表示する箇所（担当者マスタの拠点選択、マイページの登録詳細モーダルなど）は、新車・中古車それぞれの拠点設定を横断して正しい拠点色を表示するように内部処理を調整しています。

**お知らせバナーの色をテーマ連動に変更**
- お知らせ帯の背景色を、これまでの固定色（アンバー）から、設定ページ「テーマ設定」で選んだテーマのアクセントカラーに連動するように変更しました。テーマを切り替えると、お知らせ帯の色も自動的に変わります。
- 文字色は、個別に色を指定していない部分については、背景色（テーマのアクセントカラー）の明るさを自動判定して、黒文字・白文字のうち見やすい方を自動的に選ぶようにしました（`readableInkFor_`）。お知らせ設定・カレンダー設定で個別に文字色を指定した部分（30番の部分書式機能）は、これまで通りその指定が優先されます。

### 33. お知らせバナーのアイコンを削除、設定ページで項目を追加・削除した際にスクロール位置が先頭に戻る不具合を修正

**お知らせバナーのアイコンを削除**
- 画面上部のお知らせ帯に表示していたメガホンアイコンを削除しました。

**設定ページで項目を追加・削除すると先頭までスクロールが戻ってしまう不具合を修正**
- 「拠点設定」「お知らせ設定」「カレンダー設定」「担当者マスタ」の一覧で行を追加・削除すると、画面全体を作り直す`render()`が呼ばれることでスクロール位置が設定ページの先頭に戻ってしまい、下の方の項目を連続して編集しづらい不具合がありました。
- 追加・削除の操作の前後でスクロール位置を保持し直す処理（`renderKeepingSettingsScroll_`）を追加し、行を消したり増やしたりしても、操作していた場所の近くに留まり続けるようにしました（各項目内の文字入力・色変更などはもともと画面全体を作り直さない実装のため、この不具合の対象外でした）。

### 34. 車検証PDF自動リンク機能を廃止して「車検証アプリ」への外部リンクに置き換え、登録状況の可視化、担当者マスタからの拠点自動反映、拠点設定の不具合修正

**車検証PDF自動リンク機能を廃止**
- Google Driveのフォルダを巡回して顧客名が一致するPDFを自動でリンクする機能（設定ページ「車検証ファイル設定」、定期実行トリガー、シートの「車検証リンク」列）を廃止しました。関連するコード（`autoLinkVehicleInspectionPDF_`とその補助関数、`setupPdfLinkTrigger`、PDF保管フォルダIDの保存・取得）、アプリ内の表示（カレンダーのPDFアイコン、リスト・マイページのPDFボタン、詳細モーダルの車検証欄）をすべて削除しています。新規に作成するシートには「車検証リンク」列自体を作らなくなりました（既存シートの列はそのまま残ります）。

**コントロールパネルに「車検証アプリ」への外部リンクを追加**
- 上記の代わりとして、コントロールパネル（スマホはボトムタブ）の「マイページ」の上に「車検証アプリ」を追加しました。タップすると別タブで外部の車検証アプリが開きます。
- リンク先は設定ページ「車検証アプリ設定」（権限者のみ）から変更できます。未設定（空欄）の間はこの項目自体が表示されません。

**登録状況が一目でわかるように、登録予定日が過ぎた行をグレー表示**
- カレンダー画面で、登録予定日が過ぎた（当日は含まない）日の行の背景をグレーにしました。曜日・祝日の色より優先して表示され、その日の登録がもう終わっていることが一目でわかります。
- スプレッドシート側にも同様の条件付き書式を追加し、「登録予定日」が過ぎている（当日は含まない）行全体を同じ考え方でグレーにしました。既存の「顧客名の重複」「担当者マスタ未登録」の強調表示はこれより優先されるため、グレーの行の中でも異常があるセルは引き続き目立つ色で表示されます。既存シートに反映するには、スプレッドシートのメニュー「シート設定 &gt; 全シートにテンプレートを再適用」を実行してください。

**スプレッドシートで担当者を入力すると拠点が自動反映**
- db_登録データ系シートの「担当者」列に、担当者マスタ（設定ページ「担当者マスタ」）に登録済みの氏名を入力すると、同じ行の「拠点」列にその担当者の拠点が自動で反映されるようにしました（`onEdit`の単純トリガーのため、追加の設定操作は不要です）。複数セルへの貼り付けにも対応しています。一致する氏名が担当者マスタに無い場合は何も変更されません。

**拠点設定に「その他」が表示されない不具合を修正**
- 拠点設定の一覧に「その他」（どの拠点にも一致しないデータの受け皿）が表示されず、アプリ画面（KPIタイルなど）には出ているのに設定画面からは色を変えられない不整合がありました。
- 「その他」を拠点マスタの通常のデータとして常に一覧の最後に含めるように変更し、設定ページでも表示・色の変更ができるようにしました（名前の変更・削除はできないよう保護しています）。あわせて、これまでクライアント側で「その他」の色を無条件に既定色へ上書きしていた実装上の不具合（カスタム色を設定しても反映されない）も修正しました。

### 35. 権限者アカウントを最大5名まで登録できるように

- Code.gsの`EDITOR_EMAILS`に、2〜5人目用のコメントアウトされた入力欄をあらかじめ用意しました。追加したい場合は該当行の`//`を外してメールアドレスに書き換えるだけで済みます（これまで通りコードを直接編集する運用のまま、デプロイの更新は必要です）。

### 36. 担当者マスタに「拠点長」設定を追加し、拠点長アカウントのマイページに拠点全体の登録状況を表示

- 設定ページ「担当者マスタ」の各行（新規追加行にも）に、担当車両区分とは別に「拠点長」チェックボックスを追加しました。スプレッドシート側の`settings_マイページ連携`シートにも「拠点長」列が追加され、チェックボックス形式で表示されます。
- 「拠点長」にチェックが入ったアカウントでログインすると、マイページに自分の担当分だけでなく、担当者マスタで登録した**自分の拠点全体**の登録状況（新車・中古車合算、他の担当者の案件も含む）が表示されるようになります。一覧には誰の案件かひと目でわかるよう「担当者」列も追加されます（スマホ表示では幅の都合で省略され、タップして開く詳細画面で確認できます）。検索欄も顧客名に加えて担当者名でも絞り込めるようにしました。
- 拠点が未設定のまま「拠点長」だけチェックした場合、マイページには何も表示されません（拠点も必ず選択してください）。

### 37. マイページのKPI（受注・登録済・未定・OSS登録率）を新車・中古車で分けて、確実に一列で表示

**新車・中古車を分けて表示**
- これまで新車・中古車を合算した1つのKPI行（受注台数・登録済み・未定・OSS登録率）を表示していましたが、「新車」「中古車」それぞれのラベル付きで2つのKPI行に分けました。案件一覧（下の表）はこれまで通り新車・中古車を横断して表示されます。

**KPIが一列に並ばず縦に積み重なってしまう不具合を修正**
- このKPI行は、外部から読み込むTailwind CSS（CDN）のクラス（`grid grid-cols-4`など）に横並びレイアウトを依存していました。ネットワーク環境によってこのCDNの読み込みが失敗する場合、横並びのスタイルが一切適用されず、4つのタイルが縦に積み重なって表示されてしまう不具合がありました。
- カレンダー画面のKPI行（総登録台数など）と同じ、アプリ自身が持つCSS（外部CDN不要）でレイアウトするように変更し、CDNの読み込み状況によらず必ず一列で表示されるようにしました。あわせて、スマホ表示で自動的に横スクロール可能な帯状のレイアウトへ切り替わる仕組み（カレンダー画面のKPIと同様）や、右端までスクロールすると自動で消えるスクロール矢印も、この行に対して初めて正しく動作するようにしました（これまでは矢印が表示されていても効かない状態でした）。

### 38. スプレッドシートの入力方法を改善（担当者・登録予定日）、備考欄のキーワードでカードの背景色を変更、カレンダーの罫線を少し濃く

**マイページKPIを行の右端まできっちり広げて表示**
- 「新車」「中古車」それぞれのKPI行（前項37番で追加）は横一列には並んでいたものの、タイルが行の右端まで広がりきらず、右側に余白が残ってしまうことがありました。レイアウトの土台となるCSSを、空きがあってもマス目を確保してしまう方式から、実際のタイル数に合わせて幅を配り切る方式に変更し、常に行いっぱいに広がるようにしました。

**担当者列をプルダウン選択方式に変更**
- スプレッドシートの「担当者」列を、自由入力ではなく、設定ページの「担当者マスタ」に登録した氏名一覧からプルダウンで選ぶ方式に変更しました。担当者マスタの登録内容（氏名の追加・変更・削除）は、テンプレートを再適用しなくても自動的にプルダウンの選択肢に反映されます。
- 一覧にない名前が直接入力・貼り付けされた場合に備えて、これまで通り担当者マスタに存在しない名前はセルの背景が薄い赤になる警告表示も残しています。

**登録予定日列をカレンダー選択方式に変更**
- 「登録予定日」列に日付の入力規則を設定し、セルをクリックするとGoogleスプレッドシート標準のカレンダーピッカーが表示されて日付を選べるようにしました。誤入力（全角数字や区切り文字の違いなど）を防ぎやすくなります。表示形式も `yyyy-mm-dd` に統一しています。

**備考欄のキーワードに応じてカードの背景色を変更**
- 備考欄に「完成検査切れ」という文字列が含まれる案件は、カレンダー・未定リストのカード背景をイエローにしました。
- 備考欄に「〜県登録」という文字列（例:「愛知県登録」）が含まれる案件は、カード背景をグリーンにしました。
- どちらの場合も、背景色に合わせて文字色を専用の濃い色に切り替え、視認性を保つようにしています。

**カレンダーの罫線を少し濃く**
- カレンダー画面の日付ごとの区切り線を、これまでよりわずかに濃い色に変更しました。他の画面の区切り線（設定ページなど）には影響しないよう、カレンダー専用の色として調整しています。

**デバッグ結果**
- Code.gs・Index.htmlの構文チェック（`node --check`）、HTMLタグ・CSSブロックの対応チェックはいずれも問題ありませんでした。
- Playwrightによる自動テスト（PC/iPhone SE/iPhone 13の各画面幅）で、カレンダー・案件一覧・未定リスト・マイページ・設定ページの主要な操作（月移動、案件詳細の開閉、検索、拠点設定の追加・削除、テーマ切り替えなど）を確認し、コンソールエラー・横スクロールのはみ出しはいずれも発生しませんでした。
- 備考欄キーワードによるカード背景色の変更、マイページKPIの一列表示についても、実際の画面表示とスタイルの計算値の両方で意図通りの結果を確認しました。
- 担当者列のプルダウン化、登録予定日列のカレンダーピッカー化については、Googleスプレッドシート本体が提供する機能（入力規則）を利用しているため、コード上の設定内容は確認できるものの、実際のプルダウン・カレンダーピッカーの見え方はこの開発環境（実際のスプレッドシートに接続できない検証環境）では直接確認できませんでした。デプロイ後、実際のスプレッドシートで担当者列・登録予定日列をクリックし、想定通りプルダウン／カレンダーが表示されるか一度ご確認ください。

### 39. 車検証アプリ連携を廃止、備考欄「中古車新規/継続」でカードをピンク表示、登録日の背景グレーを調整、今日の行をハイライト表示、アイコン設定を改善

**車検証PDFに関する連携を廃止**
- コントロールパネル（スマホはボトムタブ）の「車検証アプリ」項目と、設定ページの「車検証アプリ設定」カードを削除しました。外部の車検証アプリへのリンクを開く導線自体をなくしています。
- 関連するコード（リンク先URLの保存・取得、設定画面の入力欄と保存ボタン、ナビゲーションの表示分岐）をすべて削除しました。スプレッドシート側には元々この機能に関わる列は残っていなかったため、シートの変更はありません。

**備考欄に「中古車新規」「中古車継続」を含む案件はカードをピンクに**
- 既存の「完成検査切れ」（イエロー）「〜県登録」（グリーン）と同じ仕組みで、備考欄に「中古車新規」または「中古車継続」という文字列が含まれる案件は、カード背景をピンクにしました。文字色も専用の濃い色に切り替えて視認性を保っています。

**登録日が過ぎた行の背景グレーを、周囲の色調に合わせて少し薄く**
- カレンダーで登録予定日が過ぎた日の行に敷いているグレーの背景を、これまでよりわずかに明るい色調に調整しました。白いカードや区切り線の色調に近づけ、過去の日付であることは分かりやすいまま、圧迫感を抑えています。

**今日の行を、ハイライトのようにはっきり表示**
- これまで「今日」の行は、コントロールパネルの選択中タブなど他の要素と同じ薄い色味だったため目立ちにくいことがありました。今日の行だけ背景をやや濃いめのテーマカラーに変更し、行全体を縁取る枠線を追加して、マーカーで線を引いたようにひと目で分かるようにしました。選択しているテーマ（インディゴ／グリーン／アンバーなど）に応じて色が自動で変わります。

**サイドパネルアイコン設定を改善**
- 画像アップロード時に、ブラウザ側で自動的に一辺512pxまで縮小してからサーバーに送信するようにしました。大きな写真をそのまま選んでも、保存されるファイルサイズ・通信量は常に一定に収まります（アイコンは最終的に小さな円形表示になるため、画質面でも十分です）。
- 保存先のDriveフォルダを指定する欄に、これまではDriveフォルダIDのみ入力可能でしたが、DriveフォルダのフルURL（`https://drive.google.com/drive/folders/...`）を貼り付けた場合も、自動的にID部分だけを取り出して保存するようにしました。IDのみの入力もこれまで通り利用できます。

**デバッグ結果**
- Code.gs・Index.htmlの構文チェック（`node --check`）、HTMLタグ・CSSブロックの対応チェックはいずれも問題ありませんでした。
- Playwrightによる自動テスト（PC/iPhone SE/iPhone 13）で回帰確認を行い、月移動・検索・拠点設定・テーマ切り替えなど既存機能に問題がないことを確認しました。
- 新しいカード配色（ピンク）、今日の行のハイライト表示、登録日超過行の背景の明るさ、設定ページから車検証アプリ関連の項目が消えていることは、実際の画面表示とスタイルの計算値の両方で確認しました。
- アイコン画像の自動縮小は、2000×1400pxの疑似画像をアップロードし、サーバーに送信される直前のデータが512px以下に縮小されていることをスクリプトで確認しました。
- DriveフォルダURL→ID自動抽出は、実際のGoogle Driveに接続できないこの検証環境のため、抽出ロジック単体のテストで正しく動作することを確認しました。実際のフォルダURLでの保存は、デプロイ後に一度ご確認ください。

## ファイル構成

| ファイル | 内容 |
|---|---|
| `Code.gs` | `doGet` エントリポイント、`onOpen`カスタムメニュー、権限者判定、車両区分(新車/中古車)ごとのスプレッドシート読み取り、車検証PDF自動リンク、テーマ/アイコン/お知らせ/カレンダー設定/担当者マスタの保存、シートテンプレート適用 |
| `Index.html` | 画面（新車/中古車カレンダー/リスト/未定リスト/マイページ/設定/登録内容の詳細表示） |
| `appsscript.json` | GASプロジェクト設定（`executeAs: USER_ACCESSING`） |

## セットアップ

1. `npm i -g @google/clasp` → `clasp login`
2. このフォルダで `clasp create --type webapp --title "登録カレンダー"` （または既存プロジェクトに `clasp push`）
3. `Code.gs` の `EDITOR_EMAILS` に、設定ページの管理項目（サイドパネルアイコン設定・車検証ファイル設定・お知らせ設定・カレンダー設定・担当者マスタ）を操作できるGoogleアカウントを追加
4. デプロイ後、権限者アカウントでログインし、設定ページの「サイドパネルアイコン設定」カードからサイドパネルアイコン画像を保存するDriveフォルダのIDを入力・保存（コード編集は不要）
5. 同じく設定ページの「車検証ファイル設定」カードから、新車・中古車それぞれの車検証PDF保管フォルダのDrive IDを入力・保存（コード編集は不要）。フォルダ内は `2026.08` または `2026.8` のような年.月フォルダで分けて運用してください（サブフォルダは何階層でも自動探索されます）
6. GASエディタから `setupPdfLinkTrigger` を一度だけ手動実行し、新車・中古車それぞれのPDF自動リンクの定期実行トリガー（15分おき）を設置
7. 既存の `db_登録データ` シート（新車・中古車とも）がある場合は、GASエディタから `refreshAllSheetTemplates` を一度実行し、入力ガイドの注釈・プルダウン・重複/担当者マスタチェック書式を適用。中古車用シートを新規に始める場合は `createNextMonthSheetUsedCar` で最初の月のシートを作成
8. GASエディタの「デプロイ」→「ウェブアプリ」で、アクセスできるユーザーを組織内に限定してデプロイ

## 未対応・今後の検討事項

- `EDITOR_EMAILS` はコード内の配列管理のため、非エンジニアが追加/削除するにはコード編集＋再デプロイが必要です。将来的にはスプレッドシートの管理シートで管理者を管理する方式への変更も検討できます。
- お知らせバナーの「閉じる」は表示中のセッション（render()を呼び直すまで）のみ有効で、ページを再読み込みすると再度表示されます。恒久的に非表示にする機能は未実装です。
- カレンダー設定・担当者マスタの日付/氏名入力欄は、入力の妥当性（日付形式や重複行など）を厳密にはチェックしていません。誤った内容を保存すると「保存しました」の表示のまま反映されるため、保存後にカレンダー/マイページ側の見え方を確認することをおすすめします。
- 現状、閲覧そのもの（顧客データの読み取り）はデプロイのアクセス設定（ドメイン内）に委ねています。閲覧できる人の範囲も絞りたい場合は別途ご相談ください。
- 既存データのステータス値は新しい3択（登録書類到着待/登録書類到着済/登録予定日確定）へ手動での一括置換が必要です（上記14.参照）。
- 担当者マスタは「担当者」欄の文字列とフルネームの完全一致で紐づけます。表記ゆれがあると該当ユーザーのマイページに何も表示されず、エラーも出ません（スプレッドシート側では担当者マスタ未登録の警告表示で気づけます）。設定画面に「どの担当者名にも紐づいていない一覧」を出す等の改善は未実装です。
- 中古車カレンダーを新規に使い始める場合、`db_登録データ_中古_...` シートにデータが1件も無いうちはマイページ・未定リストとも空表示になります。運用開始前に `createNextMonthSheetUsedCar` 等でシートを用意してください。
- 同姓同名の重複チェックは完全一致のCOUNTIFによるスプレッドシート側の条件付き書式です。全角/半角や法人格表記のゆれ（例:「株式会社」と「(株)」）までは検出しません。
- 860px未満の画面幅ではレイアウトの作り込みを最小限に留めています（固定ビューポートを解除するのみ）。スマホでの利用頻度が高い場合は、実機確認のうえ追加調整をおすすめします。
