import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "証明情報を確認 | 無加工証明写真",
};

export default function VerifyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
